// import { atom, update, createStore } from 'nanostores'

// import { useStore } from "xi-store";

// export const currentUser = atom<{ username: string } | null>(null)

// export const counter = createStore(() => {
//     counter.set(0)
// })


